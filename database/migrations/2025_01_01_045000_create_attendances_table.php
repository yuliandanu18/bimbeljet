<?php
use Illuminate\Database\Migrations\Migration;use Illuminate\Database\Schema\Blueprint;use Illuminate\Support\Facades\Schema;return new class extends Migration {public function up(): void {Schema::create('attendances', function(Blueprint $t){$t->id();$t->foreignId('enrollment_id')->constrained('enrollments')->cascadeOnDelete();$t->date('class_date');$t->enum('status',['present','late','absent'])->default('present');$t->text('notes')->nullable();$t->foreignId('checked_by')->nullable()->constrained('users');$t->timestamps();$t->unique(['enrollment_id','class_date']);});}public function down(): void {Schema::dropIfExists('attendances');}};